# セキュリティルールとワークフロー（機密情報漏洩対策）

パブリックリポジトリでの機密情報漏洩（APIキー、パスワードなどの流出）を未然に防ぎ、被害を最小限に抑えるため、本プロジェクトでは以下のルールと多層的なワークフローを定義します。

## 1. 基本ルール（コーディング規約）

*   **ハードコードの禁止**: ソースコード内に API キー、データベース接続文字列、秘密鍵、パスワードなどの機密情報を直接記述してはいけません。
*   **環境変数の活用と分離**:
    *   機密情報はすべて環境変数として扱い、ローカル開発時は `.env` ファイルに記述します（`.gitignore` にて除外済み）。
    *   新しい環境変数を追加した場合は、必ずキー名のみを記載した `.env.example` を更新・コミットしてください。
*   **権限の最小化（本番・開発の分離）**:
    *   ローカル開発で使用するAPIキーやデータベース権限は、**本番データにアクセスできない開発専用の権限（最小権限）**を持ったものを必ず使用してください。

## 2. GitHub の標準セキュリティ機能の活用（必須）

パブリックリポジトリで利用可能な以下の GitHub ネイティブ機能を必ず有効化・設定します。

### A. Secret Scanning（シークレットスキャン）
*   **概要**: GitHub がリポジトリをスキャンし、コミットされた機密情報を検知する機能です。
*   **設定**: パブリックリポジトリではデフォルトで有効になっています。「Settings」>「Code security and analysis」から有効状態を確認してください。
*   **効果**: 主要プロバイダ（AWS, GCP, Stripe等）のキーが漏洩した場合、プロバイダ側に通知され、自動的に無効化（Revoke）される保護が働きます。

### B. Dependabot（依存関係の脆弱性管理）
*   **概要**: 使用しているパッケージ（`package.json`等）の脆弱性を検知し、修正のための Pull Request を自動作成します。
*   **設定**: 「Settings」>「Code security and analysis」にて、以下を「Enable」にします。
    *   Dependabot alerts
    *   Dependabot security updates

### C. ブランチ保護ルール（Branch Protection）
*   **概要**: 誤操作や悪意のあるコードの混入を防ぐため、`main` ブランチを保護します。
*   **設定**: 「Settings」>「Branches」から `main` ブランチに対してルールを追加し、以下を有効にします。
    *   `Require a pull request before merging` (直接のPushを禁止し、PR作成を必須化)
    *   `Require approvals` (他者のレビューを必須化。※個人開発の場合はチェックを外しても可)

## 3. 開発時の自動化ワークフロー (Pre-commit)

手元の操作ミスを防ぐため、ローカルでのコミット前スキャンを導入します。

*   **使用ツール**: `husky` + `gitleaks` (または `secretlint`)
*   **動作**: `git commit` を実行した際、自動的にスキャンが走り、機密情報らしき文字列を検出した場合はコミットを強制的にキャンセルします。

## 4. アプリケーション設計のセキュリティ

*   **ログ出力のマスク処理**:
    *   本番環境等でエラーが発生した際、ログファイルに機密情報が出力されないようにします。
    *   ロガー（Winston, Pinoなど）を設定し、`password`, `token`, `secret`, `api_key` などのプロパティを `***` にマスク処理（Redact）するよう実装してください。
*   **本番環境のシークレット管理**:
    *   本番環境では `.env` ファイルを使用せず、ホスティングサービス（Vercel, GitHub Actions Secretsなど）や、専用のシークレットマネージャー（AWS Secrets Manager等）の機能を使用してください。

## 5. インシデント発生時の対応フロー

万が一、機密情報をパブリックリポジトリに Push してしまった場合は、以下の手順を**直ちに**実行します。

1.  **無効化 (Revoke) 【最優先】**:
    *   漏洩したAPIキーやパスワードを、各サービスの管理画面から即座に無効化・再発行します。
    *   *注: 一度公開された情報は既に悪意あるボットにコピーされている前提で動くため、Git履歴からの削除よりも無効化が最優先です。*
2.  **履歴からの抹消**:
    *   BFG Repo-Cleaner などのツールを使用し、Gitのコミット履歴から該当ファイルを完全に抹消し、強制 Push (`git push -f`) します。
3.  **事後レビュー**:
    *   漏洩経路を特定し、ローカルの検知ツール（`secretlint` 等）のルールをアップデートして再発を防止します。
