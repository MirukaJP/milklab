# Design Harness Constitution (DESIGN.md)

このドキュメントは、本プロジェクトにおける「デザインハーネス（メタ・デザインシステム）」の憲法（Layer 1）です。
人間のエンジニアおよびAIアシスタントは、デザインに関わる全ての実装において、本ドキュメントの原則と `contracts/` の仕様に必ず従う必要があります。

## 1. 哲学 (Philosophy)
本システムは、「特定のUIライブラリ」や「単一のビジュアルデザイン」を構築するものではありません。
どのような技術スタック（React, Vue, Vanilla JS, Swift等）や、どのようなブランドテーマ（ダーク、ライト、ガラスモフィズム等）にも適用・派生可能な「**デザインの根幹アーキテクチャ**」を構築します。

## 2. 3層アーキテクチャ (Architecture)
システムの一貫性は、以下の3層構造によって維持されます。

* **Layer 1: 憲法 (DESIGN.md)** - 本ドキュメント。思想と大原則。
* **Layer 2: 仕様 (Contracts)** - `/design/contracts/` に配置される仕様ファイル群。デザイン意図の「唯一の真実情報 (Single Source of Truth)」。
  * ※ プロジェクト（ブランド）個別のコンセプトや意図については、必ず [design/contracts/brand.md](file:///Users/Miruka/workspace/milklab/design/contracts/brand.md) を参照すること。
* **Layer 3: 検証 (Harness)** - `/harness/` に配置されるスクリプト群。Contracts の制約を実装コードに対して検証するエンジン。

## 3. AI / エージェント・ディレクティブ (AI Directives)
AIアシスタント（エージェント）がこのプロジェクトでコードを記述する際、以下のルールを**例外なく**遵守すること。

1. **仕様 (Contracts) の絶対視**: コンポーネントの実装やデザインスタイルの適用は、必ず `/contracts/` 以下の JSON 仕様に基づかなければならない。
2. **勝手なスタイル定義の禁止**: 個別のコンポーネントファイル内に直接 `margin` や `padding` のハードコード（マジックナンバー）、場当たり的な色の指定を行ってはならない。
3. **Contracts 変更の手続き**: 仕様にない新しいコンポーネントや状態 (State) を追加したい場合は、必ず先に `/contracts/` 内の JSON ファイルを更新（または作成）し、承認を得てから実装すること。
4. **検証 (Harness) の通過**: 生成したコードは、常に Harness (Layer 3) の Lint/Validation を通過するものでなければならない。
5. **曖昧な命名の排除 (Explicit Naming)**: トークンなどのキー名・変数名において、一般的なプログラミング用語や既存概念（`property` などの略語）と混同を招く省略（例: `prop`）を行ってはならない。意図が正確かつ唯一に解釈できる名称（例: `proportional` や単位名 `em` 等）を使用すること。
6. **仕様外の装飾・ノイズの禁止 (Proscription of Ad-hoc Ornamentation)**: 視覚的な目新しさを作るためであっても、対象ブランドの Contracts に定義されていないグラデーション、多色の混在、余分な境界線、無意義なアニメーションをコード実装時に勝手に追記してはならない。

## 4. コア原則 (Core Principles)
* **分離の原則**: スタイルの定義（Tokens）と、構造・振る舞い（Components）は完全に分離される。
* **決定論的テーマ**: 全ての色やサイズは、関数やトークンマップを通じて一意に決定されなければならない。
* **アクセシビリティ・ファースト**: ARIA属性、キーボードナビゲーションなどの a11y 仕様は後付けではなく、Contracts 層から強制される。
* **プラットフォーム不変性と等価的セマンティクス (Cross-Platform Equivalence)**: トークンは「CSSの即値」にとどまってはならない。空間充填を表す `auto` のような抽象キーワードも設計トークンとして定義する。これにより、Webでの `margin: auto` や `flex` 制御はもちろん、iOS (SwiftUI) の `Spacer()` や Android (Jetpack Compose) の `Modifier.weight(1f)` といったネイティブ層のレイアウト論理へ、等価・安全にトランスレーションできる構造を維持する。
* **定義のブランド移譲 (Delegation of Scaling Strategy)**: 本システムは特定のスケール手法（厳格な数理グリッド主義や、錯視・流動を併用するハイブリッド主義など）を全ての中核として強要・固定化しない。具体的な余白やカラーの構成アプローチは全て Layer 2（各ブランドごとの `brand.md` と `contracts`）の定義へと移譲され、本システム（AIおよび Harness）はそこで選ばれた「そのブランド固有の哲学」を絶対の正解として厳密に守り、自動検証する柔軟な器として機能する。
